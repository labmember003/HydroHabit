package com.falcon.hydrohabit.ui.theme

import androidx.compose.ui.graphics.Color

// ── Apple-inspired palette ──────────────────────────────────────────
// Backgrounds
val backgroundColor1 = Color(0xFFFFFFFF)          // pure white
val backgroundColor2 = Color(0xFFF2F2F7)          // iOS system grouped background

// Text
val primaryBlack = Color(0xFF000000)                  // primary label
val primaryBlackLight = Color(0x99000000)              // secondary label (60 % black)

// Accent — iOS system blue, used sparingly
val waterColor = Color(0xFF007AFF)                 // system blue
val waterColorBackground = Color(0x0A007AFF)       // very faint blue tint for backgrounds
val circleWaterIndicatorColor = Color(0xFF007AFF)  // accent for indicators
val waterColorMeter = Color(0xFF007AFF)            // accent for cursors / meters

// Buttons & cards
val buttonTextColor = Color(0xFFFFFFFF)            // white text on dark surfaces
val blackShadeColor = Color(0xFF1C1C1E)            // iOS system dark card
val textFieldColor = Color(0xFFFFFFFF)             // white button / surface
val onboardingBoxColor = Color(0xFFF2F2F7)         // light gray card
val textFieldErrorColor = Color(0xFFFF3B30)        // iOS system red



